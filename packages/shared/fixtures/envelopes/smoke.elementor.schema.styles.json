{
  "$fixture": 1,
  "id": "smoke.elementor.schema.styles",
  "kind": "smoke",
  "generation": "v4",
  "title": "Inspector smoke payload — minimal valid input for the lean ★ elementor.schema.styles tool",
  "_comment": "WP-Q03 — Inspector smoke (14-fixtures-harness.md §8; 13-tool-catalog.md §5.2 #9, §1.1). schema.styles is READ-ONLY (readOnlyHint/idempotentHint; proxied-to-REST GET elementor-ultra/v1/schema/styles) — the flat atomic Style-Schema (css-prop → prop-type), units, states. The Style-Schema is an atomic-v4 surface, so requires.experiments gates this on e_atomic_elements (a v4-disabled site SKIPS). MINIMAL valid input per §1.1: the inputSchema is the empty object {} ⇒ `arguments` is {}. The §8 suite asserts no protocol error (-326xx) and a result matching the outputSchema {props, units, states[]}.",
  "expect": {
    "valid": true,
    "errors": []
  },
  "requires": {
    "experiments": ["e_atomic_elements"],
    "pro": false,
    "min_elementor": "4.1.1"
  },
  "tool": "elementor.schema.styles",
  "arguments": {}
}
