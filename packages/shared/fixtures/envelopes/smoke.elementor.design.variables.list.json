{
  "$fixture": 1,
  "id": "smoke.elementor.design.variables.list",
  "kind": "smoke",
  "generation": "v4",
  "title": "Inspector smoke payload — minimal valid input for the lean ★ elementor.design.variables.list tool",
  "_comment": "WP-Q03 — Inspector smoke (14-fixtures-harness.md §8; 13-tool-catalog.md §5.2 #15, §1.4). design.variables.list is READ-ONLY (readOnlyHint/idempotentHint; proxied-to-REST GET elementor-ultra/v1/design/variables; paginated §0.6) — design tokens (color/font/size, all FREE in 4.1.x) + optimistic watermark. Variables are a v4 feature, so requires.experiments gates this on e_variables (a variables-disabled site SKIPS). MINIMAL valid input per §1.4: only §0.6 pagination ({limit?,cursor?,fields?}); we set a small `limit`. The §8 suite asserts no protocol error (-326xx) and a result matching the paginated outputSchema {items[], next_cursor, total, watermark}.",
  "expect": {
    "valid": true,
    "errors": []
  },
  "requires": {
    "experiments": ["e_variables"],
    "pro": false,
    "min_elementor": "4.1.1"
  },
  "tool": "elementor.design.variables.list",
  "arguments": {
    "limit": 5
  }
}
