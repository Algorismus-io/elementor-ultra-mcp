# Honor WordPress .htaccess (pretty permalinks) and forward the HTTP Authorization header to PHP,
# so application-password Basic auth reaches WordPress. Without this, /wp-json auth returns 401.
<Directory /var/www/html>
    AllowOverride All
</Directory>
SetEnvIf Authorization "(.*)" HTTP_AUTHORIZATION=$1
